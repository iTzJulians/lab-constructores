const prompt = require("prompt-sync")();
 function Vehiculor(marca, modelo, velocidad, ruedas, precio) {
    this.marca = marca;
    this.modelo = modelo
    this.ruedas = ruedas;
    this.precio = precio;
}
let 
